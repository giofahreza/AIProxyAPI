name: Deploy to Oracle Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.24'

      - name: Build binary
        run: |
          GOOS=linux GOARCH=amd64 go build -o aiproxyapi cmd/server/main.go

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.ORACLE_SSH_KEY }}" > ~/.ssh/oracle.key
          chmod 600 ~/.ssh/oracle.key
          ssh-keyscan -H ${{ secrets.ORACLE_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy to server
        run: |
          # Copy binary to server
          scp -i ~/.ssh/oracle.key aiproxyapi ${{ secrets.ORACLE_USER }}@${{ secrets.ORACLE_HOST }}:~/AIProxyAPI/aiproxyapi.new

          # SSH into server and deploy
          ssh -i ~/.ssh/oracle.key ${{ secrets.ORACLE_USER }}@${{ secrets.ORACLE_HOST }} << 'ENDSSH'
            cd ~/AIProxyAPI

            # Backup and replace binary
            if [ -f aiproxyapi ]; then
              cp aiproxyapi aiproxyapi.backup.$(date +%Y%m%d_%H%M%S)
            fi
            mv aiproxyapi.new aiproxyapi
            chmod +x aiproxyapi

            # Restart service
            sudo systemctl restart aiproxyapi.service

            # Check status
            sleep 2
            sudo systemctl status aiproxyapi.service --no-pager
          ENDSSH

      - name: Verify deployment
        run: |
          echo "✅ Deployment complete!"
          echo "🔗 Service should be running at http://${{ secrets.ORACLE_HOST }}:8317"
